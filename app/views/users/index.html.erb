<% provide(:title, "Lista Uytkowników") %>

<h1>Lista użytkowników</h1>

<%= will_paginate %>

<ul class="list-group">
  <% @users.each do |user| %>
  <li class="list-group-item"><%= link_to user.username, user %></li>
  <% end %>
</ul>

<%= will_paginate %>